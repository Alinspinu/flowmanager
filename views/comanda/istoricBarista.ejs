<% layout('layouts/boilerplate') %>
    <div class="row">

        <% comenzi.forEach((el)=>{%>
            <div class="col-lg-3 mt-3">
                <ul class="list-group">
                    <li class="list-group-item">
                        Nr. Comandă <%=el.index%>
                    </li>
                    <li class="list-group-item"><strong style="font-size:19px;">Produse:</strong></li>
                    <% el.produse.forEach((el)=>{%>

                        <li class="list-group-item">
                            <%=el.cantitate%> X <%=el.nume%>
                        </li>

                        <% }) %>
                </ul>
            </div>
            <% }) %>
    </div>